[package]
name = "opensnow-catalog"
description = "Catalog layer: database/schema/table metadata, Iceberg REST catalog client (Apache Polaris), and warehouse registry."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
opensnow-common = { path = "../opensnow-common" }
anyhow = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

# TODO: add iceberg-rust once implementation begins
# iceberg = "..."
