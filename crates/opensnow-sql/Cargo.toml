[package]
name = "opensnow-sql"
description = "Snowflake SQL dialect extensions on top of Apache DataFusion: VARIANT type, QUALIFY, AT/BEFORE, UNDROP, semi-structured data functions."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
opensnow-common = { path = "../opensnow-common" }
opensnow-catalog = { path = "../opensnow-catalog" }
anyhow = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

# TODO: add when implementation begins
# datafusion = "..."
# sqlparser = "..."
