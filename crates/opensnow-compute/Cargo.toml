[package]
name = "opensnow-compute"
description = "Compute layer: virtual warehouse manager, DataFusion query execution engine integration, result caching."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
opensnow-common  = { path = "../opensnow-common" }
opensnow-catalog = { path = "../opensnow-catalog" }
opensnow-storage = { path = "../opensnow-storage" }
opensnow-sql     = { path = "../opensnow-sql" }
anyhow = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
datafusion = "53"
datafusion-common = "53"

# TODO: add when implementation begins
# datafusion = "..."
# arrow = "..."
